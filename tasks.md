# Tasks: Python Diary App Implementation

- [x] **Setup**: Initialize project structure and create the `diary.json` template.
- [x] **Data Layer**: Implement a module to load and save entries to the JSON file safely.
- [x] **Write Functionality**: Implement the command to take user input (via argument or prompt) and save it with a timestamp.
- [x] **View Functionality**: Implement a command to list recent entries with formatted output.
- [x] **Habit Tracking**: Implement streak logic that increments on consecutive days and resets if a day is skipped.
- [x] **Stats Command**: Create a summary view showing total entries and the current streak.
- [x] **Refinement**: Add error handling for corrupted JSON files or invalid inputs.